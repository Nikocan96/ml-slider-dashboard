name: Refresh Dashboard

on:
  schedule:
    - cron: '0 * * * *'   # cada hora en punto (UTC)
  workflow_dispatch:        # botón manual desde GitHub

jobs:
  refresh:
    runs-on: ubuntu-latest

    env:
      GCS_BUCKET: ml-slider-dashboard   # <-- cambiá esto por el nombre de tu bucket

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Authenticate to GCP
        run: |
          echo "${{ secrets.GCP_SA_KEY }}" | base64 -d > /tmp/gcp-sa-key.json
          echo "GOOGLE_APPLICATION_CREDENTIALS=/tmp/gcp-sa-key.json" >> $GITHUB_ENV

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: pip

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Generate dashboard
        run: python generate.py

      - name: Upload to GCS
        uses: google-github-actions/upload-cloud-storage@v2
        with:
          path: docs/index.html
          destination: ${{ env.GCS_BUCKET }}
          parent: false
